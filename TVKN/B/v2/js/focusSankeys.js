// function filterNodes (data) {
//   return data.filter(node => node.legend === 'hydrogen' || node['target.id'] === 'waterstofproductie')
// }

// setTimeout(() => { // TODO: TOKENIZE
//   console.log(links)

//   console.log(filterNodes(links))
// }, 1000)

// let sankeyData_waterstof = {links: [],nodes: nodes,order: sankeyData.order}
